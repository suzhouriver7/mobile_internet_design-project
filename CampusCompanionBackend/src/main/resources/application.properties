spring.application.name=CampusCompanionBackend

# 数据库连接配置
spring.datasource.url=jdbc:mysql://localhost:3306/campus_companion?useUnicode=true&characterEncoding=utf-8
spring.datasource.username=root
spring.datasource.password=879879
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 服务器配置
server.port=8080
# 注意：不要在这里再加 /api/v1 前缀，否则会变成 /api/v1/api/v1/...
# 统一由各个 Controller 的 @RequestMapping("/api/v1/xxx") 来控制接口前缀

# 日志配置
logging.level.root=info
logging.level.dev.campuscompanionbackend=debug

# JWT配置
jwt.secret=your-secret-key
jwt.expiration=3600000
jwt.refresh-expiration=7200000

spring.mail.host=smtp.163.com
spring.mail.port=465
spring.mail.username=campuscompanion@163.com
spring.mail.password=TMrBnbvtMaXJb9cp
spring.mail.ssl.enabled=true
spring.mail.protocol=smtps
# 文件上传大小限制（根据前端实际需求适当调整）
# 单个文件最大 20MB
spring.servlet.multipart.max-file-size=20MB
# 单次请求（所有文件总和）最大 100MB
spring.servlet.multipart.max-request-size=100MB
